{
  "name": "client-registration-api",
  "version": "1.0.0",
  "description": "Backend API for client registration and user data management using Supabase",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest --watchAll --verbose",
    "test:coverage": "jest --coverage",
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "format": "prettier --write ."
  },
  "keywords": [
    "api",
    "supabase",
    "registration",
    "user management",
    "backend"
  ],
  "author": "Your Name <your.email@example.com>",
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^2.40.0", // Supabase JavaScript client library
    "bcryptjs": "^3.0.2", // For password hashing (consider updating to the latest version)
    "body-parser": "^1.20.2", // For parsing request bodies
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^5.1.0",
    "express-validator": "^7.0.1", // For input validation
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.0",
    "prettier": "^3.2.5"
  }
}
